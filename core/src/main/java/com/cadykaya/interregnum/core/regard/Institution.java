package com.cadykaya.interregnum.core.regard;

/**
 * Who is keeping score. There is deliberately no single "morality" here: a
 * morality number is false specificity (docs/AESTHETIC.md), whereas a god
 * remembering what you said to them is a relationship. See docs/WORLD.md
 * "Dialogue -- No karma bar".
 */
public enum Institution {
    WARDENATE,
    VILLAGES,
    VERDANT,
    ANCHORITE,
    HEARTH_TURNER,
    QUIET_ONE,
    /** The dead god itself -- only its killer can move this one. */
    THE_GHOST
}
